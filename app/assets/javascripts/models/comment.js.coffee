class Streamaweb.Models.Comment extends Backbone.Model

  idAttribute: '_id'
  

#  save: (attrs) ->
#    @id = attrs.id
#    options = {}
#    attributes = {description: "test", id: @id}
#    Backbone.Model.prototype.save.call(this, attributes, options)

